# 2026-09-19

## 公众号 CSP 分数线预估文章（进行中）
- 文章《2026 年广东省 CSP-J/S 第一轮晋级分数线预估》已通过 CDP 填入后台并存为草稿 `appmsgid=100000291`（标题/作者 Alan张老师/摘要已设）。
- **正文坑位记录（重要）**：
  - 技能 `cdp_wechat.js` 硬编码 `TOKEN='1690824997'` 已过期 → 有效 token 为 `1102226852`（从已登录 mp 首页标签 URL 提取）。token 在重复/陈旧导航后会失效，**必须动态从已登录首页标签取 token 并一次性导航**。
  - ProseMirror 正文填充实测结论：`insertHTML` 会丢全部表格；合成 `ClipboardEvent` paste 大段会被丢弃（只有首块存活、顺序错乱、表格重复成 7 张）。**可靠做法是真实剪贴板 + 真实 Ctrl+V**（`navigator.clipboard.write` + CDP 授权 + Input.dispatchKeyEvent Ctrl+V），此路最后一步被中断，下次继续。
  - 当前草稿 100000291 正文仍是错乱版本（缺"一、结论先行"+首表，表格 7 张），需重跑填充修复。
- 探测脚本沉淀在同目录：`_probe_state.js`（列 CDP 标签渲染态）、`_verify.js <appmsgid>`（按 appmsgid 精确校验草稿）、`csp_fill.js`（填充主脚本）。

## 封面图（已完成）
- 用 ImageGen 生成科技感底图（1280x544，深蓝→青渐变+折线趋势图，无文字），PIL 合成 1024x436 封面：
  - 右下角 "AI生成 WORKBUDDY" 水印用「左侧同 y 带干净区 clone + 高斯模糊」覆盖；
  - 左侧加暗色渐变 scrim 保证标题可读；标题「CSP-J/S 2026初赛晋级线预估」用 msyhbd.ttc 58px 白字+阴影，青色竖条 accent，副标题「广东省 · 第一轮 · 分数线预测」。
- 产出：`.../wechat-mp-cdp-publish/scripts/content/csp-scoreline/cover_csp_scoreline.png`；合成脚本 `make_cover.py`（改文案即可复用）。
- 环境坑：沙箱 pip 装不了 pillow（内外镜像都不通）→ 系统 Python 3.11 自带 Pillow 12.2.0 直接用；bash shim 缺 grep/tail/ls/cat，用 node/fs 和 findstr 替代。
