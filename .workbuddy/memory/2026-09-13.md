# 2026-09-13

## 项目逻辑梳理（两次）

上午 + 下午连续两轮为 ai-math-mistake-machine 项目做了一次系统梳理，结论是**项目实际是三产品线**，不是我记忆里说的"两个"：

1. **StudyMate / 作业通**（`aijiangti.cn`，小学 4-6 年级数学错题讲解机，backend MVP + frontend OpenMAIC 改造）
2. **CSP 真题训练 / 王牌战队2**（`/csp-lecture`、`/classroom/[id]`，第三方 Vjudge-AI-report C++ 接入已落地，10 个学员档案）
3. **少年 AI 创造营**（`edu.xgteacher.cn`，7-12 岁 AI 启蒙营，Alan张老师个人品牌，XIAOGAO LAB 主体，三张业务表 camp_students/camp_class_logs/camp_works 上线于 2026-08-30，工具栈走 Trae IDE）

下午第二轮是用户提醒"没有看到少年AI创造营吗"才补齐的——我上午把 camp 路由 + works-wall 静态镜像 + admin/camp/* 后台全漏了，原因是我按 SKILL.md 的"4-6 年级数学"框架走，没看 `app/page.tsx` 首页菜单和 `lib/db.ts` 里的 camp 表。

教训：下次梳理"产品矩阵"型项目时，先 grep 一遍项目目录的产品关键字（品牌名、域名、备案号），再决定讲几条线，不能光看 SKILL.md 决定。

## Bug 修复：管理后台上传的学员作品不在少年 AI 创造营展示

**根因**：公开侧完全没有读库链路。
- `frontend/app/camp/works/page.tsx` 和 `frontend/app/camp/work/[slug]/page.tsx` 原来都是**硬编码静态数组**（`WORKS`），从不访问数据库。
- 项目里只有带鉴权的 `/api/admin/camp/works`，**没有公开读取接口**（`frontend/app/api/camp` 此前不存在）。
- `camp_works` 表无 `slug` 字段；详情页还依赖库里没有的富文本（创作记录 lessons、能力雷达 radar）。
- 管理端"新增作品"POST 不传 status，默认 `pending`，须经审核（点"通过"）才应公开。

**修复**（已落地，未跑 next dev 因为沙箱 safe-delete shim 拦了 `.next/dev/trace` 删除导致进程被杀；改用 better-sqlite3 直连 data/studymate.sqlite 验证了公开查询 SQL）：
1. 新增 `frontend/app/api/camp/works/route.ts`：GET 公开，只返回 `status='approved'`，顺序 `featured DESC, sortOrder ASC, createdAt DESC`。
2. 新增 `frontend/app/api/camp/works/[id]/route.ts`：GET 公开，按 id 取单条，非 approved 一律 404。
3. 重写 `frontend/app/camp/works/page.tsx`：改读 `/api/camp/works`，叠加 2 个静态示范作品（炳炳/小高）作为种子；加载/空态/错误态齐备。
4. 重写 `frontend/app/camp/work/[slug]/page.tsx`：slug 命中静态示范作品走原完整页；否则按 id 拉 `/api/camp/works/[id]`，缺 lessons/radar 的区块自动隐藏。

**验证结论**：插入 approved 作品→公开查询返回；pending 作品不泄漏；临时数据已清理。注意：用户上传后需到 `/admin/camp/works` 点"通过"审核，作品才出现在公开作品墙（这是"审核后公开"的设计，不是 bug）。

**复跑提示**：完整 `next dev` 在本沙箱会被 safe-delete shim 拦崩（`.next/dev/trace` 批量删除触发 SAFE_DELETE_BULK_CONFIRM_REQUIRED）。若要在本机验证 UI，需关闭该 shim 或换非 WorkBuddy 沙箱环境跑 `pnpm dev`。

## Bug 修复：管理后台点"通过"审核提示"作品不存在"

**根因**：项目升级到 **Next.js 16.1.2** 后，App Router 的 route handler 里 `params` 变成了 **Promise**，但 `frontend/app/api/admin/camp/works/[id]/route.ts` 的 GET/PATCH/DELETE 还按老写法直接解构 `const { id } = params;`，导致 `id` 实际是 `undefined`（或 Promise 对象），`db.campWork.findUnique({ where: { id } })` 查不到记录，于是返回 404"作品不存在"。

**影响面**：不只 camp/works，所有带 `[id]` 的动态路由 handler 都有同样隐患。我一次性修了 6 个文件共 11 个 handler：
1. `frontend/app/api/admin/camp/works/[id]/route.ts` — GET/PATCH/DELETE
2. `frontend/app/api/admin/camp/students/[id]/route.ts` — GET/PATCH/DELETE
3. `frontend/app/api/admin/camp/logs/[id]/route.ts` — GET/PATCH/DELETE
4. `frontend/app/api/camp/works/[id]/route.ts` — GET（本次新增）
5. `frontend/app/api/integrations/jobs/[id]/route.ts` — GET
6. `frontend/app/api/integrations/jobs/[id]/retry/route.ts` — POST

**改法**：把 handler 签名里的 `params: { id: string }` 改成 `params: Promise<{ id: string }>`，并在函数体第一行 `const { id } = await params;`，后面全部改用 `id` 而不是 `params.id`。

**验证**：TypeScript 层面没有 `params: { id:` 残留；由于沙箱 safe-delete 问题未跑完整 `next dev`。需要用户重新部署 frontend 后到线上点"通过"验证。

**教训**：Next.js 15+ App Router 的动态路由 `params` 是异步 Promise，不只是 page.tsx，route.ts 的 handler 也必须 await。以后新增或审查 `[id]` 路由时，第一件事就是检查 `await params`。

## 创造营品牌文案调整（用户直接指令）

按用户要求，对少年 AI 创造营对外文案做了 4 处调整：
1. **工具栈加 WorkBuddy**：`frontend/app/camp/page.tsx` 的 TOOL 区块原来只有 Trae，现加 WorkBuddy 品牌 chip（`.workbuddy-brand`）+ 文案"Trae ... WorkBuddy 是 Alan张老师的 AI 助手工作台，把课程、作品与一键部署串成一条线"。`shared.css` 补了 `.workbuddy-brand` / `.tool-plus` 样式。
2. **删除备案主体 XIAOGAO LAB**：移除所有页面 footer 的 `苏ICP备2025178103号-2` / `苏公网安备32041202003961号` / `© 2025 XIAOGAO LAB`。涉及 camp/page.tsx、camp/prepare/page.tsx、camp/works/page.tsx、camp/work/[slug]/page.tsx、works-wall/*.html（index/prepare/works/work-detail-1/2）。版权行按用户决策"直接删掉整行"。
3. **xgls 代码简称弃用 → alan- 前缀**：资产文件 `frontend/public/assets/xgls-avatar.png`、`xgls-logo.svg` 经 `git mv` 重命名为 `alan-avatar.png`、`alan-logo.svg`；所有 `/assets/xgls-*`、class `.xgls-brand`、远程微信图 `https://edu.xgteacher.cn/assets/xgls-avatar.jpg` 一并改为 `alan-*`。
4. **历史成绩更新**：`200+ 学员作品 / 5 年项目教学经验` → `2000+ 学员作品 / 15 年项目教学经验`（camp/page.tsx 的 about 区块数字与文案）。

**注意项（需用户在服务器端处理）**：远程微信图 `https://edu.xgteacher.cn/assets/alan-avatar.jpg` 指向线上服务器，代码已改 URL，但**服务器上的文件仍需用户手动从 `xgls-avatar.jpg` 重命名为 `alan-avatar.jpg`**，否则公开页微信二维码会 404。本地 `/assets/alan-*` 已随 git 提交。

改动已 commit + push 到 master（frontend 重建即可生效）。

## 新功能：学生自助提交作品 + 老师审核上墙

用户希望学生能自己上传作品到少年 AI 创造营作品墙，老师只审核。实现：
1. **db.ts 幂等迁移**：将 `camp_works.studentId` 由 `NOT NULL` 改为可空（SQLite 不支持 ALTER COLUMN DROP NOT NULL，用 `PRAGMA table_info` 检测 `notnull=1` 后「改名→新建→复制→删旧」重建表；用 BEGIN/COMMIT 包裹）。原因：学生自助提交时往往没有 `camp_students` 档案，用 `studentName` 承载姓名。
2. **`frontend/app/api/camp/works/route.ts` 新增公开 `POST`**（无登录）：入库即 `status='pending'`；防护 = 单 IP 10 分钟 8 次限流（`checkRateLimit` from `lib/integrations/rate-limit.ts`）+ 隐藏蜜罐字段 `company`（命中静默成功不入库）；校验 title/studentName 必填、URL 字段须 http(s)、长度上限。复用 `db.campWork.create`。
3. **`frontend/app/camp/submit/page.tsx`** 新学生提交页（Tailwind 表单 + 提交成功提示，含蜜罐隐藏域）。
4. **`frontend/app/camp/works/page.tsx`** 作品墙头部加「我也来上传作品」入口 → `/camp/submit`。

**审核流不变**：学生 `/camp/submit` 提交 → 老师 `/admin/camp/works` 点「通过」→ 上墙 `/camp/works`。

**验证**：用 better-sqlite3 直连临时库复刻迁移 SQL，确认 `studentId.notnull` 由 1→0、NULL 提交入库成功、approved/pending 隔离正确（脚本已删）。已 commit + push 到 master（dfb0639）。

**部署**：服务器 `cd /home/ubuntu/studymate && git pull origin master && docker compose up -d --build frontend`（用户已用此命令）。限流依赖进程内 Map（容器重启清零，足够小站）。

## 风格改造 + 学生信息加年级字段

用户报 `/camp/submit` 风格和营地其它页（`/camp`、`/camp/works`、`/camp/work/[slug]`）的"便签纸+米黄格子底+navy墨色"对不上，并要求学生信息里加"年级"。

**根因**：之前的 submit 页是用 Tailwind 琥珀渐变（`bg-gradient-to-b from-amber-50 to-orange-50`），跟营地 `shared.css` 的 `.works-page` 米黄格子纸感风（`var(--paper)` + 44px 格子底 + `--navy-950` 边框 + `--yellow` 强调 + masking tape）是两套体系。

**改动（commit 32bff15，6 文件 +304/-109）**：
1. **`/camp/submit` 重写为同款纸感设计**：用 `.works-page` 米黄格子底 + sticky `.works-header`（品牌+导航复用）+ 紧凑 `.submit-intro`（`SUBMIT YOUR WORK / 03` 标头 + 大字 h1 + 引言）+ `.submit-card` 便签纸表单卡（sharp navy border、轻微 -0.6deg rotate、`.work-note-tape` masking tape、`.submit-error` 错误条）+ 纸感输入（`.submit-input` / `.submit-textarea` / `.submit-select`，`var(--paper)` 底 + navy border + focus 内嵌下划线 + 自定义 caret 三角）+ `.submit-button-primary` 黄底 navy墨色按钮（hover 反色）+ `.submit-success` 黄底便签纸成功卡。表单拆 STEP 01 学生信息 / STEP 02 作品信息两块，加 `.submit-divider` 分隔线。
2. **`shared.css` 新增 submit 系列样式**：`.submit-intro / -shell / -card / -card-head / -card-intro / -grid / -divider / -field / -field-label / -field-req / -field-hint / -input / -textarea / -select / -error / -honeypot / -actions / -button-primary / -button-secondary / -fineprint / -success / -success-actions / -success-link / -kicker / -kicker--success`，含 980/640px 响应式覆写（小屏单列、卡片去 rotate、按钮全宽）。
3. **`camp_works` 加 `grade TEXT` 可空列**：新部署走 CREATE TABLE（init 块 + applyMigrations 的 re-run 块两处都加）；老库平滑迁移走 `applyMigrations` 里的 `ALTER TABLE camp_works ADD COLUMN grade TEXT`，try/catch 包住吞掉"duplicate column"实现幂等。`buildFinder.create` 用 `Object.keys(data)` 动态拼 INSERT，`grade` 自动写入。
4. **`POST /api/camp/works` 接收 `grade`**：白名单 `{一年级, 二年级, 三年级, 四年级, 五年级, 六年级, 不便透露}`；缺失/非法一律回退到「不便透露」；写库走 `db.campWork.create`。
5. **作品墙 + 详情页 `studentLabel` 改为 `[studentName, grade].join(' · ')`**：原 `[studentName, className]` 改为年级（与用户截图需求对齐，班级信息仍存库但暂不进 label，避免卡片文字过长）。`DbWork` 类型补 `grade: string | null`。

**验证**（用 managed node 22 + better-sqlite3 直连临时库）：
- 老库（无 grade 列）→ 新 init 块的 CREATE TABLE IF NOT EXISTS 是 no-op → ALTER ADD COLUMN 补上 grade ✓
- 二次 ALTER 被「duplicate column name: grade」catch 吞掉，幂等 ✓
- 写读一致：`grade='三年级'` 插入后能读回 ✓
- 未填年级存为 NULL ✓

**关键设计判断**：年级白名单只到六年级，对应营地"7-12 岁"画像（初一已 12-13，超出营地定位）。需要时可加。`不便透露` 作兜底默认值，既不报错也不泄露"未填"空状态，对学生友好。

**已 commit + push 到 master**（commit `32bff15`）。部署仍是 `cd /home/ubuntu/studymate && git pull origin master && docker compose up -d --build frontend`。

**待办（用户曾在更早一轮问的、暂搁置）**：
- 学生上传 HTML 文件 → 自动生成介绍+封面 → 二次编辑。决策已收齐（内联沙箱预览 + AI 插画+真实截图都要 + 匿名 token 编辑链接），但用户这一轮没继续推进。如果回来做，建表再加 `htmlFile / editToken / coverSource` 三列；编辑/上传走 `/api/camp/works/[id]/html` GET（CSP sandbox 头）+ multipart POST 落 `data/camp-uploads/<id>.html`（需新增持久卷挂载）；自动介绍走 fire-and-forget `callLLM`；自动封面走 fire-and-forget `generateImage(seedream)`。

## 作品扫码分享（微信/朋友圈）+ 分享卡片 metadata

用户要求"作品支持扫码分享微信和朋友圈"。实现（commit `4173056` + `81f7c6e`，已 push）：

1. **`work/[slug]/page.tsx`**：把「分享作品」按钮从 `navigator.share` 改为弹扫码弹窗。新增 `ShareModal` + `ShareQr` 组件，客户端**动态 `import('qrcode')`** 按需生成当前作品 URL 的二维码（512px、白底 navy 色块、errorCorrection M），附「复制链接」按钮 + 文案「微信扫一扫打开后点右上角 ··· 分享给朋友/朋友圈」。`window.location.href` 作为二维码内容（部署后即 `https://edu.xgteacher.cn/camp/work/<id>`）。
2. **`work/[slug]/layout.tsx`（新增，server 组件）**：导出 `generateMetadata` 生成 og:title/description/image，让微信/朋友圈分享卡片有正确标题描述封面（否则是光秃秃链接）。种子作品用静态元信息镜像（SEED_META，2 个硬编码）；DB 作品动态读 approved 行（`title/description/coverImage` + `studentName·grade` 拼 description）。`export const dynamic='force-dynamic'` 避免构建期触库；`getDb` 走**动态 import** 惰性加载。
3. **`types/qrcode.d.ts`（新增）**：qrcode 包无自带类型的 ambient 声明，避免引入 `@types/qrcode` 再动 lockfile。
4. **`shared.css`**：新增 `.share-overlay/.share-modal/.share-modal-tape/.share-qr-card/.share-qr/.share-copy-btn` 等纸感弹窗样式（米黄纸卡 + masking tape + sharp navy 边框 + 黄底 hover），对齐 works 设计语言。
5. **`package.json` + `pnpm-lock.yaml`**：新增 `qrcode@1.5.4`（纯 JS 零原生依赖）。

**顺手修 bug（commit `81f7c6e`）**：`lib/integrations/rate-limit.ts` 只 `import { RateLimitedError } from './errors'` 未 re-export，导致 `POST /api/camp/works` 里 `import { RateLimitedError }` 拿到 undefined，触发限流时 `err instanceof RateLimitedError` 抛 TypeError（500 而非 429）。补 `export { RateLimitedError }`，同时消除 tsc 的 TS2459/TS18046。

**验证**：`tsc --noEmit` 下本功能相关文件零报错（qrcode ambient 声明生效、layout generateMetadata 类型正确）。项目 `next.config` 有 `typescript.ignoreBuildErrors: true`，既有的 `app/camp/page.tsx`（canvas ctx null）与 `app/admin/camp/logs/page.tsx`（日期对象属性）类型错误不影响 build。

**踩坑**：`pnpm add qrcode` 在这个大 monorepo 上卡 12 分钟（触发全量 node_modules link），实际 lockfile 已正确写入 qrcode 条目后被我 kill；改为**手动补 package.json 的 `"qrcode": "1.5.4"`（精确版本，与 lockfile specifier 一致）** + 自写 ambient d.ts 完成，无需再跑 pnpm。教训：加纯 JS 无类型依赖时，可手动改 package.json + 复用 pnpm 已写好的 lockfile 条目 + 自写 d.ts，避免全量 install。

**部署提醒**：本次加了新 npm 依赖（qrcode），服务器构建 `pnpm install --frozen-lockfile` 会自动装。部署命令不变：`cd /home/ubuntu/studymate && git pull origin master && docker compose up -d --build frontend`。
