# 2026-09-17

## 作品关联教材功能（camp 作品墙 / 提交 / 编辑 / 详情）
- 需求：学生上传作品须选择「关联教材」（必填 · 单本），详情页显示教材标签，作品墙支持按教材筛选。
- 实现面：DB camp_works.textbookSlug（db.ts 三处 CREATE TABLE + applyMigrations 幂等补列 + 启动自检 required）；API /api/camp/works POST 必填校验+列表 ?textbook= 过滤、/api/camp/works/edit/[token] 读写、/api/camp/works/[id] SELECT * 自动带出；前端 submit/edit 加必填下拉（optgroup 按科目），works 加教材筛选下拉（客户端过滤，种子作品自动排除）+ 卡片 📘，work/[slug] hero 显示「关联教材：xxx」。
- 辅助：lib/textbooks.ts 的 groupTextbooksBySubject()/textbookTitle(slug) 复用。
- 行为变更：编辑页保存现强制要求选教材——老作品（textbookSlug=null）首次编辑保存前须先选一本，否则拦截提示。
- 类型检查：tsc --noEmit 基线 54 条（均无关文件），本次 0 新增。
- 部署：git push 后服务器 git pull && docker compose up -d --build frontend；迁移由 applyMigrations 自动补列。本地沙箱跑不了 next build，构建放服务器。

## 公众号评论回复（9/19 早）
- 老胡 / LoBo 两条评论回复话术已给：先认对方对的部分，再讲「正因为 AI 基于概率不保证每次都对，我们才让孩子把作品贴出来让人判断和迭代」——把弱点转成产品价值。无需改代码。

- 提交 d3d6edb 已 git push 到 origin/master（15 文件，730 增 / 411 删）；远端 SHA 与本地 HEAD 对账一致，确认真实推送（d0ed622..d3d6edb）。
- 服务器部署待执行：cd /home/ubuntu/studymate && git pull origin master && docker compose up -d --build frontend（applyMigrations 自动补 textbookSlug 列，无需手跑 SQL）。
- 提交时未 git add -A：仓库根目录有大量未跟踪的调试脚本/PDF/OCR 图（.workbuddy/_diag*.py、J雪峰*/、frontend/public/figures/* 等）属历史 scratch，保持未跟踪，未混入本次提交。
