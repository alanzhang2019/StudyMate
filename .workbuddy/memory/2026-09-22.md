# 2026-09-22 工作日志

## 深圳中考真题库接入「深圳教材」页（续 09-19）
- 已落地（本地+仓库）：page.tsx 新增「教材/中考真题」切换分区（科目 tabs + 年份筛选 + 仅下载卡片，无在线预览）；lib/examPapers.ts（171 条，含科目/年份/卷型/文件名/大小）；app/api/exam-papers/[...slug]/route.ts 流式下载（附件头 + 防目录遍历）。
- 数据：171 份 doc/docx（7 科 2008-2025，去重后）拷贝至 frontend/data/exam-papers/<subject>/，已 scp 到服务器 /home/ubuntu/studymate/frontend/data/exam-papers（链式 && echo ALL_SENT 已完成；远端计数未二次核验，因被用户中断）。
- 提交 a1dcaeb（9 文件），ls-remote 对账推送真实。tsc 基线 54，新文件 0 错误。
- **未完成**：服务器前端尚未 git pull + next build + 重启容器，故线上新分区/下载未生效。
- **阻塞**：本会话 Bash 工具持续报「command expected string」校验错误、PowerShell 空输出且沙箱拦 SSH，无法连服务器。Shell 恢复后需补：① 核验远端 171 文件数 ② git pull ③ next build ④ 重启 studymate-frontend。
- 注意：data 目录 gitignore，不进仓库，必须 scp；源「知识点归纳汇总」非年份真题已排除。

## 部署完成（15:21 收尾）
- 服务器 git 已在 a1dcaeb，171 份文件齐全。镜像 `studymate-frontend:latest` 用 `docker compose build frontend`（**带缓存，非 --no-cache**）重建（2 天前缓存命中，绕开 better-sqlite3 下载超时——截图那个失败是另一台机器 OrbStack 上 `--no-cache` 全量重建导致的）。
- 重启：`docker compose up -d --no-deps --force-recreate frontend`（原样复用 compose 配置，不动数据卷）。
- **关键踩坑**：容器 `/app/data` 是**命名卷 `studymate_studymate-frontend-data`**，不是宿主机 `frontend/data` 的 bind。scp 到宿主机的 `frontend/data/exam-papers` 容器内看不到 → 路由 500/404。正确做法：`docker cp frontend/data/exam-papers studymate-frontend:/app/data/exam-papers`（写进命名卷，随卷持久、重建不丢）。已 cp 171 份入卷。
- 线上实测：/textbooks 页面含「中考真题」切换按钮（卡片为客户端渲染，点击后出）；`/api/exam-papers/math/math-2008-answer.docx` 返回 200 + 附件头（中文文件名 `数学 · 2008 · 真题及答案.docx`）。功能已生效。
- 注意：2024 数学只有 analysis/blank 两卷型，无 answer 卷（源里就没有），路由对不存在文件正确返回 404，属正常。
- 遗留增强（非必须）：建议把 exam-papers 纳入 deploy-prod.sh 的 cp/挂载步骤，避免未来重部署遗漏；或加 bind 挂载替代 docker cp。

## 把 exam-papers 同步固化进 deploy-prod.sh（15:39 完成）
- 已改服务器 `deploy-prod.sh` 并提交推送：新增 **第 7/9 步**「Sync 深圳中考真题库 into named volume」，在重建容器后从宿主机 `frontend/data/exam-papers` 用 `docker cp` 把真题同步进命名卷 `/app/data/exam-papers`，并核对文件数（host vs volume）。顺带把脚本步数标号统一为 N/9、更新头部清单与 help 范围。
- 提交：`ec07ca3`（新增步骤）→ `17143c2`（修 docker cp 嵌套 bug）。均 ls-remote 对账真实推送。
- **关键踩坑（已修）**：`docker cp SRC CONTAINER:/app/data/exam-papers` 在目标目录**已存在**时会把源目录**嵌套**成 `/app/data/exam-papers/exam-papers/`，导致每轮部署真题翻倍（实测一次变 342，容器内默认 exec 用户是 nextjs 非 root，`rm -rf` 还报 Permission denied，需 `docker exec -u 0`）。正确写法：先 `docker exec studymate-frontend mkdir -p /app/data/exam-papers`，再 `docker cp "$EXAM_SRC/." "studymate-frontend:/app/data/exam-papers/"`（仅拷内容，幂等 upsert 不嵌套，实测重复执行稳定 171 份）。
- 验证：volume 当前 171 份、无嵌套目录；线上 `/api/exam-papers/...` 仍 200、/textbooks 含「中考真题」。
- 数据拓扑提醒：exam-papers 走命名卷而非 bind，故 deploy 必须靠脚本 docker cp 兜底（`deploy-prod.sh` 第 7 步），否则重部署/换卷会丢数据。
