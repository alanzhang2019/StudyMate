# 2026-09-18

## 公众号首篇：已填入后台草稿（未发表）

**成果**：用 CDP 接管用户已登录的 Chrome（9222 调试端口），把《四十二年，它一直是最优解》完整填进公众号后台并**保存为草稿**，`appmsgid=100000322`。

落地状态（status 命令核验）：
- 标题《四十二年，它一直是最优解》/ 作者 Alan张老师 / 摘要 93 字
- 正文 4306 字、4 张配图全部上传成功并转存微信 CDN（mmbiz.qpic.cn）
- 封面已设置（`has_first_cover=true`）
- **未点发表**，等用户确认

**待用户确认后**：点「发表」；若提示公众号未实名（保存时 toast 出现过该提示）需先实名。

## 草稿 v2 同步（当晚）：第二节末新增「热情更稀缺」论述

用户补充核心观点：**信奥最宝贵的是 42 年来培养自主学习能力的最佳实践；AI 时代自主学习能力仍然重要，而持续学习的热情更加稀缺。**

改动（md/html 同步）：
- 第二节末（「驾驭」段之后）新增 8 段：信奥最宝贵的不是金牌而是**培养自主学习能力的最佳实践** → AI 时代该能力反而更重要 → 但「能不能学下去」之外更稀缺的是「还想不想学下去」 → 金句块「**能力可以练，热情只能被保护。**」
- 第三节开头衔接句改为「当那个前提开始消失，**当热情变得比能力更稀缺**……」，直接承接第三节「培养探索欲/保护好奇心」。
- 正文 4306 → 4675 字；已重灌后台草稿（复用同一 target，封面/摘要保留）并保存，编辑器显示「已保存」。

**重灌草稿要点**：`cdp_wechat.js all` 会清空正文重灌 + 重新上传 4 图（CDN 重复但无害）；复用 `_cdp_target.json` 的 targetId 即同一草稿。重灌后页面可能残留「选择图片」素材库弹窗，`document.dispatchEvent(KeyboardEvent Escape)` 可关。给 save 的截图加了 try/catch（截图超时不再 FATAL，保存动作本身不受影响）。新增 `js` 通用求值命令。

## 隐私约定（用户要求）

**公开物料中孩子姓名只写名不写姓**（灏洋/焯麟/浩峻），不展示完整姓名。配图 4 三联图图注已按此重渲染（fig4.html + fig4-works.png）；正文文字本来就是名不带姓。作品墙本身仍是全名展示（那是站点既有设计，未动）。用户表示公众号更新由他自己操作。

## 精简去 AI 味改稿（用户看过草稿后反馈「太长 + AI 味」）

**诊断（已获用户确认）**：4675 字偏长（家长完读甜蜜区 2200-2800）；AI 味六病灶：①一句话一段 ②悬念式破折号×11 ③加粗金句堆叠 ④脚手架引导词（「请注意」「我的答案是」）⑤对仗句成瘾 ⑥AI 式首尾呼应。

**改稿原则：砍议论、留事实；砍修饰、留细节。**
- 7 节 → 4 节（一、它曾经是最优解 / 二、变的是前提 / 三、我们在做的三件事 / 四、三个孩子+把话说明白+结尾）
- 「四十二年」7 次 → 1 次；破折号 11 → 2；加粗 ~30 → 8（4 处结构性 + 3 处金句）；删炳炳段（与开头重复）、老树例子、「我尊敬这条路」复述
- 保留：灏洋老师评语原文、三节课过程记录、金牌 54/国集 50、NOI 论坛「以 AI 为镜」、「不能加分才是干净出口」、诚实段
- 成稿 3314 字符（后台显示 3019 字），比原稿 -30%

**二维码**：文末 CTA 新增真实微信二维码（从服务器容器 `/app/public/assets/alan-wechat-qr.jpg` docker cp 取回，images/alan-wechat-qr.jpg），以 `<img>` 进正文由脚本上传。

**重灌草稿踩的两个大坑（已固化进 skill `wechat-mp-cdp-publish`）**：
1. **清空正文**：execCommand 和 Range+Delete 在 ProseMirror 上全部无效（假成功、实际追加），唯一有效 = JS focus + CDP 真实按键 **Ctrl+A + Delete**；清空后必须 collapse 光标，否则传图会替换掉全部内容（曾 9 图→1 图）。
2. **保存草稿后编辑器变 contenteditable=false（只读态）**：paste 全失效但传图仍成功（图走工具栏按钮）→ 修复 = 重新导航编辑 URL 重载页面。诊断：列出所有 .ProseMirror 的 contenteditable/len/imgs。
脚本已加 `clear`/`js` 命令 + 粘贴 after>before 校验。

**草稿最终状态（同 appmsgid=100000322）**：标题/作者/摘要 93 字/封面 v3 均保留；正文 3019 字 + 5 图（配图 1-4 + 文末二维码）；已保存未发表。

## 技术要点（已固化为 skill `wechat-mp-cdp-publish`）

- **不要用 `playwright.connectOverCDP`**：Chrome 152 上 WS 连上后握手卡死（30s timeout）。改用 **Node 全局 WebSocket 手写 CDP**，零依赖、稳定。
- 公众号新版编辑器是 **ProseMirror**（无 UEditor iframe）：正文 = `.ProseMirror[1]`，标题 = `textarea#title`，作者 `#author`，摘要 `#js_description`。
- 正文 HTML 用 **ClipboardEvent paste** 注入（`DataTransfer` setData text/html + text/plain），内联样式能保留。
- 图片/封面上传 = `el.click()` 开弹窗 → `DOM.querySelectorAll('input[type=file]')` 取**最后一个** nodeId → `DOM.setFileInputFiles` → 等 10-12s。
- 正文必须**文本块与图片交替处理**（先 paste 全文再补图会把图全堆到文末）。

## 封面 v3（用户两轮反馈后定稿方向）

v1「高墙变门槛」→ 恐怖感 + 缺计算机/AI 元素；v2「机器人+发光屏幕」→ **AI 味重**；
v3：手绘水彩绘本风，AI 不作为物件出现，用孩子身后**水彩晕染的半透明人形轮廓**表达人机共生。
**教训**：面向家长的封面，① 隐喻进信息图、封面放理想画面；② 「AI 味」= 机器人/发光/全息/科技蓝，表达共生要走**非科技媒介 + 抽象含蓄的存在感**；③ prompt 显式写「不要塑料感/3D 渲染感/商业科技插画感」。

## 白名单赛事调研：哪些适合 AI 项目（7-12 岁软件作品）

用户问「教育部白名单到底哪些适合人工智能项目参赛」，针对创造营场景（孩子用 WorkBuddy/Trae 做教学可视化网页/小游戏）核实结论：

**47 项逐层筛 → 真正对口仅 4 项**（绝大多��是硬件或现场答题，不收软件作品）

| 赛事 | 序号 | 关键对口点 |
|---|---|---|
| **全国青少年人工智能大赛**（中国福利会+中国妇女发展基金会，2025-2028 新增） | #18 | 五赛道中**「人工智能工具应用大赛」是唯一小学可参加的"用 AI 工具做东西"赛道**。首届 2026（4-5 月初赛，8 月上海决赛，4.6 万报名/856 决赛），宁夏有小学生拿该赛道小学组一等奖 |
| **全国青少年人工智能创新挑战赛 NOAI**（中国少年儿童发展服务中心） | #1 | 小学低/高年级组分组；「数字化与虚拟仿真创新方向」下 AI 智能体开发、虚拟仿真平台创新设计（第八届还有生成式 AI 应用、3D 编程模型）；**线上选拔 + 上传作品 + 视频连线答辩**，不用跑现场 |
| **全球发明大会 ICC**（中国友好和平发展基金会） | #17 | G/P/J/S 组，1-4 人队，10 主题含**「AI 与生活」**；参赛语言写"图形化编程、Python"；交**发明原型 + 发明日志 + 查新报告**；官方口径"关注全过程，不以完美成品定结果" |
| **全国青少年人工智能辅助生成数字艺术创作者大赛**（文旅部艺术发展中心，艺体类） | #45 | 小学到中职，AIGC 数字艺术，孩子用 AI 生成的封面/插画/可视化可投 |

**重要排除项**：**全国青少年科技创新大赛 #14 仅普通高中+中职，小学不能参加**（很多人以为可以）；NOI 及五大学科奥赛、丘成桐奖均仅普通高中；AILD #29 智能设计有「AIGC 技术应用」「AI 视觉设计」赛项但以答题为主；世界机器人大会信息素养类 #2 的算法应用主题赛是**线上答题**（客观题+编程操作题），不收作品。

**时间窗口**：2026 赛季全部结束（8 月决赛已办完），下一轮 **2027 年 3-6 月报名 / 7-8 月决赛** → 整整一学年准备期，正好配合翻转课堂跑一轮。

**最强论点（给文章第三步用）**：这些赛事评审方式的共同点是考**"讲清我为什么这么定"**——ICC 要发明日志、NOAI 要专家问辩、人工智能大赛强调解决真实问题。**这恰好是翻转课堂练的那个能力**：不是为了参赛而参赛，是同一件事的两端。

**合规红线**：白名单明文「不得作为招生入学依据、不得用于高考加分」；「严禁委托校外培训机构组织引导学生报名或代为接收报名」；ICC 明确「严禁指导教师、家长替代学生完成作品创作，严禁机构代做」→ 做培训的一定不能变相收费组织参赛或代做作品。

## 第三轮改稿：基于用户自行修改后的版本再精简（3093 → 2606 字）

用户在后台草稿上**自己又改了一版**（贴回给我），主要新增/改动：
- 开头改成「我做科技教育十五年，带过两千多个孩子」+「有一句话我越来越不敢说满」
- 第一节补 NOI 青岛第 43 届、金牌 50→54、国集仍前 50、「第 51-54 名有金牌没有保送」；新增「不该在同一道题上受挫就把探索科学的梦想一起丢掉」
- 第二节新增「我的答案是：持续对新事物的热情」（替换原「理解/掌握/驾驭 AI」）
- 新增独立「三、我们要培养什么」+「四、怎么做：三步」，三步讲得更细（老树例子、6 大主题）
- 三个孩子段落扩充（焯麟"如果我能变成爬山虎"、浩峻妹妹灵感、苞米地的车）
- 结尾新增「我们要走出来的，是一条更宽的路：不筛掉谁」
- **删掉了**：诚实段（三步走到哪了）、「不能加分才是干净出口」、教材版本细节、文末 CTA 与二维码

**我的精简动作**（用户要求"在这个我修改后的基础上精简"）：
1. 合并「我们要培养什么」+「怎么做：三步」为**三、怎么做**（两段并一段，省一节）
2. 删除用户版中**重复两次**的「它没有错。四十二年，四十三届…前提正在消失」（只留第二节内一处；结尾改用"那条路没有错…更宽的路"）
3. 删「请注意，这不是说算法思维没用了」这类脚手架（用户版保留的 AI 味残留）
4. 压缩：1984 段、"凭什么最优"、"为什么是讲"、三个孩子过程记录、结尾
5. **保留用户全部新增素材**：青岛 43 届/54 枚金牌/国集 50、热情=答案、自主学习能力=42 年最佳实践、三个孩子细节、更宽的路
6. **保留了 CTA + 二维码**（用户版没有，但这是引流文，不能没有出口；已在回复中说明，不需要可删）

字数：用户版 3093 → 2606 汉字（正文约 2500，含 CTA）。第一版只压到 2816（仅 -9%，不够），返工再砍一轮到 2606。
**教训：精简必须量化核对**——改完跑一遍字数统计再交付，别凭感觉。

文件：`01-article.md`（精简版）、`01-article.html`（公众号内联样式版）、`01-article-user-draft.md`（用户修改版原稿存档，防回退）。
**状态：尚未同步后台草稿**，等用户说要不要同步（用户此前说过"你改好我来公众号更新"）。

## 第四轮：排版精修 + 同步后台草稿（用户指令"更新到公众号，做好排版"）

**排版升级**（`01-article.html` 重写）：
- 副标改为蓝色竖线引导（`border-left:3px solid #2B50A8;padding-left:8px`）
- 每节标题加 28×3px 蓝色装饰条（width+height 的空 section），标题 18px 深蓝 #16295C
- 金句块升级：#F3F6FC 底 + 4px 蓝左边框 + 17px 加粗深蓝（"名额变多了，塔尖没有"/"能力可以练，热情只能被保护"/"AI 不是替孩子学"3 处）
- **4 张配图全部加图注**（13px 灰 #9AA3B2 居中，img 包在 p 里 text-align:center）
- 文末 CTA 做成 #F3F6FC 圆角卡片（教材链接 + 加微信 + 二维码 160px + 备注提示）
- 署名改居中

**同步过程**（复用 tab，appmsgid=100000322 不变）：
- `all` 清空+灌文+传图一次成功：clear 后 len=10/imgs=0，11 块交替，图 0→5 递增，文本 0→3366 递增
- 新摘要 67 字：「信奥四十二年，一直是最好的那条路。但让它成为最优解的那个前提，正在消失。当写代码不再是壁垒，什么才是？我的答案是：持续对新事物的热情。」
- 素材库弹窗残留：Escape + 点"取消"后 `visibleDialogs=1` 仍报，但再截图确认实际已关（**注意：`visibleDialogs` 计数不可靠，以截图为准**）
- **截图 FATAL timeout 坑：标签页在后台时 `Page.captureScreenshot` 会超时**；重试（页面被 bringToFront）后成功。保存动作不受截图超时影响
- save 成功，status 复核：title/author/digest 67/imgs 5/text 3366/cover true
- toast 提示仍含「公众号尚未实名」「未设置头像和名称，发表内容需要完善」→ **发表前需先完成实名+头像名称设置**

## 作品视频系统性 404 诊断（2026-09-18 下午）

用户反馈作品页视频播放不了（例：`/camp/work/dbbd7f75-...` 戒赌模拟器）。

**实测结论（用 Python urllib 绕过 Bash curl 环境的 shim 故障）**：
- 作品 API 确认 `introVideoFile=/api/camp/videos/<id>.mp4` 字段正常设置
- **全部 8 个带视频的作品**该 URL 均返回 **nginx 直出 404**（`404 Not Found` / `nginx/1.18.0`）
- 封面 `/api/camp/covers/*.png` 正常 → 因 covers 走 Next.js 路由、不被 nginx 拦截

**根因**：2026-09-15 修复 nginx conflicting server name 后，`/api/camp/videos/` 的**直出真正生效**；但 nginx 读的宿主机目录 `~/studymate/frontend/data/camp-videos` 是**空的**——视频文件只落在容器命名卷 `studymate-frontend-data` 的 `/data/camp-videos` 里，没同步到宿主机。这正是"改卷挂载未迁移命名卷数据"的坑的再现（9-14 上传的视频当时走 Next.js 能播，9-15 直出生效后宿主机空目录 → 全 404）。

**修复脚本（初版，方向反了，已废弃）**：最初以为文件在命名卷、需迁到宿主机。

**✅ 更正后的实测根因（2026-09-18 17:21）**：
- 用户 reload 后视频仍 404。实测：视频 URL 现返回 `{"success":false,"error":"not found"}`（Next.js JSON 404，非 nginx 静态 404）→ 说明 nginx 回落到 Next.js 已生效，是**路由本身找不到文件**。
- 封面 `/api/camp/covers/<id>.png` 返回 200 → 读取路由用的 `STUDYMATE_DB_DIR/camp-covers` 存在且已填充。
- 封面与视频读取路由**同源**（`lib/server/camp-work-autogen.ts` 的 `DATA_DIR=STUDYMATE_DB_DIR`），视频 404 只可能是 `STUDYMATE_DB_DIR/camp-videos` 里没有该文件。
- docker-compose 挂载真相（line 136 vs 138）：`./frontend/data/camp-videos:/app/data/camp-videos`(136) 声明在 `studymate-frontend-data:/app/data`(138) **之前** → 后者覆盖前者，**绑定从未生效（死配置）**。
  - ⇒ 视频若上传于迁移后 → 实际写入**命名卷** `/app/data/camp-videos`（但卷内为空 → 说明不是这种情况，或上传于迁移前）
  - ⇒ 视频更可能上传于迁移前、落在**被覆盖的宿主机绑定目录** `frontend/data/camp-videos`（容器读不到）或已丢失
- **结论**：文件不在读取路由找的目录里。需服务器侧定位并归位。

**恢复脚本（更正版，已 push `871cc5d`）**：`.workbuddy/restore_camp_videos.sh` = ① 卷内 camp-videos 现状+DB_DIR ② 宿主机绑定目录现状 ③ 从作品 API 取登记文件名→核对卷内→缺失的到宿主机全盘 `find` 并拷进卷（`docker run` 挂卷+宿主机只读，只拷不删）④ curl Range 验 206。脚本已修正名字文件写 `$HOME` 的路径 bug。

**根治（方案 B 已实施，但仍暴露挂载脆弱）**：删 nginx 直出、视频走 Next.js（commit 97749ed）。docker-compose line 136 那条被覆盖的 camp-videos 绑定应删掉（死配置+注释错误"覆盖子路径"），让视频统一落在命名卷，与 covers 同套路。删除该绑定前**必须先跑恢复脚本把宿主机视频拷进卷**，否则丢失。

## 作品视频 404 根治：用户选 B 并实施（2026-09-18 晚）

用户选 **方案 B**：删 nginx 直出块，视频走 Next.js。已改 `nginx/studymate.conf`（删除 `location /api/camp/videos/`，改注释说明），**本地提交 97749ed**。

**关键再确认（读 docker-compose + 上传/读取路由定稿）**：
- 上传路由 `[id]/video/route.ts` 与读取路由 `[filename]/route.ts` 都基于 `STUDYMATE_DB_DIR=/app/data` → 读写同路径 `/app/data/camp-videos`
- docker-compose 里命名卷 `studymate-frontend-data` 声明在 `./frontend/data/camp-videos:/app/data/camp-videos` 绑定**之后**（line 138 vs 136）→ 挂载时命名卷覆盖子路径绑定 → 上传实际写进**命名卷** `/app/data/camp-videos`（= route.ts 读取处）
- ⇒ 方案 B **无需任何数据迁移**：文件已在命名卷、route.ts 已能读到；删 nginx 块后请求回落 `location /` → proxy_pass 到 Next.js:3001 → route.ts 流式+Range 返回 206，与封面完全同套路

**部署（只需服务器操作，不用重建容器，route.ts 早已部署）**：
```bash
cd ~/studymate
git pull origin master            # 取 97749ed（沙箱暂未 push，需先在能连 origin 的机器 push）
sudo cp nginx/studymate.conf /etc/nginx/sites-enabled/studymate.conf
sudo nginx -t && sudo nginx -s reload
# 验 206：
curl -s -o /dev/null -w "%{http_code}\n" -H "Range: bytes=0-1023" \
  "https://aijiangti.cn/api/camp/videos/dbbd7f75-003c-4a33-a43c-b91c6015d6d4.mp4"
```
**待办**：① push 97749ed（沙箱 `.ssh` 被拦，需在本地/能连机器 push）② 服务器跑上面 4 行 ③ 验 206 后用户在浏览器确认 8 个作品视频可播。docker-compose 那条被覆盖的 camp-videos 绑定挂载成死配置，暂留（删需重建 frontend 容器）。

**push 已成功（17:15 本机执行）**：`git push origin master` 返回 `7126a0d..97749ed master -> master`，`git ls-remote` 对账 `origin/master = 97749ed` 与本地一致。之前那次"被 `.ssh` 拦"是误判，这次沙箱放行了。
⇒ 待办 ① 完成，剩服务器 ④ `git pull` + ⑤ `cp` + ⑥ `nginx -s reload` + ⑦ 验 206 + ⑧ 用户浏览器确认 8 个作品视频。

## 恢复脚本跑完：宿主机与命名卷全空，8 个视频未找到（2026-09-18 18:01）

用户在服务器跑 restore_camp_videos.sh 结果（截图）：
- 命名卷 /app/data/camp-videos：空
- 宿主机绑定目录 frontend/data/camp-videos：空
- API 登记的 8 个视频全部「未找到」，验证全 404（size_download=37 = JSON 错误体，说明 Next.js 路由本身已通）

结论（待最后排查旧容器/孤儿卷后定）：视频从未真正持久化——9-14/15 期间容器反复 up -d --build，文件只存在于当时容器的可写层或被覆盖的挂载里，随容器重建丢失。
最后恢复手段：docker ps -a 查旧 frontend 容器，docker cp <旧容器>:/app/data/camp-videos 拷出；docker volume ls 查孤儿卷逐个 ls camp-videos。全空则只能 /admin/camp/works 重新上传 8 个视频。
长期修复：删 docker-compose line 136 死绑定，视频统一落命名卷（与 covers 同套路），上传即持久。

## 破案：视频在孤儿卷 studymate_studymate-frontend-data（2026-09-18 18:05）

用户跑排查命令，发现两个卷并存：
- studymate-frontend-data（现役，空）——当前容器挂的
- studymate_studymate-frontend-data（孤儿，**8 个 mp4 全在**，Sep 14-16）——compose 项目名曾变过导致的旧卷

恢复：docker run --rm -v studymate_studymate-frontend-data:/src:ro -v studymate-frontend-data:/dst alpine sh -c 'mkdir -p /dst/camp-videos && cp -a /src/camp-videos/. /dst/camp-videos/'，无需重启。拷完 8 个 URL 验 206。
后续：① docker-compose 删 line 136 死绑定 ② 旧卷先留作备份，稳定后再清理。

## 拷入现役卷后仍 404 → 怀疑 3001 由另一个容器服务（2026-09-18 18:09）

用户把视频拷进 studymate-frontend-data 卷（docker run alpine cp），卷内确认有文件，但 8 个 URL 仍 404。
关键线索：docker ps 里有**两个**容器：studymate-frontend-1（Up 2 days）和 studymate-qpenmic-1（Up 6 days，另一个 compose 项目的旧容器）。若 qpenmic 占着 3001（nginx 代理目标），则一直在查错误的应用。
确诊命令：docker ps 看端口绑定；docker inspect 两容器 .Mounts；docker exec 两容器 ls /app/data/camp-videos；env | grep STUDYMATE。
不依赖结论的立即修复：docker run 把卷内视频中转到 /tmp/vids，再 docker cp 进正在服务的容器 /app/data/camp-videos/（穿透写卷，立即生效）。

## docker cp 报 No such container: studymate-frontend-1（2026-09-18 18:12）

容器名认错了——真正服务 3001 的容器叫别的名字。修复改为自动定位：docker ps --filter publish=3001 拿容器名（host 网络模式则用 ss -tlnp 对 pid），再 docker cp /tmp/vids/. <容器>:/app/data/camp-videos/，验 206。需要用户回报真实容器名以做长期修复对齐。

## ✅ 视频恢复成功 + 最大真相（2026-09-18 18:14）

docker cp 进容器 studymate-frontend（169MB）后 curl 返回 206，视频恢复。

**最大真相**：真正服务 3001 的容器叫 studymate-frontend——docker run 手动起的（compose 容器名才带 -1 后缀）。仓库 docker-compose.yml 与线上实际运行的不一致，这就是卷分家/挂载错位/怎么拷都不生效的总根源。之前记忆里「部署 = git pull && docker compose up -d --build frontend」的说法需要修正。

**待办**：
1. 用户验 8 个 URL / 浏览器确认可播
2. docker inspect studymate-frontend（Mounts/Image/Ports/RestartPolicy）发我，做长期对齐：让 compose 复用同卷，或规范容器
3. 孤儿卷 studymate_studymate-frontend-data 留作备份；宿主机 /studymate-frontend-data、/tmp/vids 可清理

## inspect 定稿：compose 其实是对齐的，之前「死绑定」判断错误（2026-09-18 18:15）

docker inspect studymate-frontend 实际挂载：
- bind -> /app/data/classrooms
- bind -> /app/data/camp-videos（宿主机 frontend/data/camp-videos）
- bind -> /app/public
- volume studymate_studymate-frontend-data -> /app/data（**前缀卷才是现役数据卷**，DB/covers 都在里面）
- 3001 unless-stopped

**纠正**：Docker 挂载按目标路径深度排序，深层 bind 遮蔽卷的同名子目录 → compose line 136 的 camp-videos 绑定**不是死配置**，一直生效（视频该落宿主机）。之前「命名卷声明在后覆盖绑定」的说法错误。
**为什么曾经丢**：上传发生在容器挂载布局不同的历史时期，文件写进了前缀卷的 camp-videos 子目录，后被 bind 遮蔽读不到； docker cp 进容器 → 实际写入宿主机 bind 目录 → 206 恢复。
**之前拷的裸卷 studymate-frontend-data 是无关杂卷**（可清理）。
**结论**：docker-compose.yml 无需改动！删 line 136 反而会破坏持久化（幸好等到了 inspect）。视频现居宿主机 frontend/data/camp-videos，纳入备份即可。容器布局与 compose 一致，下次重建容器只要用同一 compose 配置即可。

## 更正：沙箱对 aijiangti.cn 的 HTTP 扫描不可信（2026-09-18 18:30）

用户指正：「实际上只有 dbbd7f75 这一个打不开」，与其余 7 个能播。我此前「7 个 404、仅 dbbd7f75 206」的扫描结论是**沙箱 http_proxy 缓存的假数据**（本机请求被劫持缓存）。再次扫描 8 个全显示 nginx 静态 404，与用户浏览器实况矛盾，进一步证实。
**结论**：该域名必须从服务器/容器内测，沙箱 urllib/curl 结果一律不可采信。dbbd7f75 单独失效 = 它缺失于宿主机 bind 目录 frontend/data/camp-videos（其余 7 个已在）。修复 = 从权威卷 studymate_studymate-frontend-data 把 8 个补齐到容器 camp-videos 的 bind 源目录。

## 数据保护盘点 + compose 红线（2026-09-18 21:20）

用户问「数据都保护了吗」。结论：数据已恢复但**未备份、卷名混乱、有被误删风险**。
数据拓扑（已据 docker-compose.yml + inspect 钉死）：
- **DB + 封面**：仅存命名卷 studymate_studymate-frontend-data（=compose 声明 studymate-frontend-data，项目名前缀）→ 容器 /app/data。不在 git、不在宿主机普通目录。删卷=永久丢失。
- **视频**：宿主机 bind ~/studymate/frontend/data/camp-videos（8 个已恢复）。
- **课件/静态**：bind + git 工作区，低危。
- 调试期我们曾建过裸卷 studymate-frontend-data（无前缀，含 8 视频冗余拷贝），现为意外第二备份，先留着。

已在 docker-compose.yml 卷声明处加删除红线（commit 074a500，本地未推）：禁止 down -v / volume rm / volume prune。

待办：① push 074a500 + 871cc5d ② 服务器跑备份快照（卷 tar + 主机 camp-videos tar）③ 列危险命令清单给用户。

## 两个提交已推 origin（2026-09-18 21:34）

871cc5d（视频恢复脚本）+ 074a500（compose 数据删除红线）均已 git push origin master（871cc5d..074a500）。git ls-remote 对账 origin/master=074a500=本地 HEAD。用户 git pull 后红线进生产配置。

## backup-data.sh / clean-disk.sh 安全审查（2026-09-18 22:43）

用户服务器跑完：backup-data.sh 手动执行成功，cron 设 `0 3 * * *`（clean-disk 行已注释禁用）。

**backup-data.sh 现状（合格）**：
- 数据卷 `studymate_studymate-frontend-data` → tar 到 ~/backups/data-*.tar.gz（每日 03:01，989M–1.2G 轮转）
- **视频备份段已生效**：campvideos-20260918-224250.tar.gz (86M) 由本次运行生成 → 宿主机 frontend/data/camp-videos 已纳入备份
- 脚本自带 `find -mtime +7 -delete` 对 data-* 与 campvideos-* 两种都轮转

**clean-disk.sh 审查结论（关键）**：
- ✅ **无 `docker volume prune`**（最担心的误删活动卷风险，实际不存在）
- ✅ `docker container prune` 只删已停止容器、不删命名卷 → 数据卷安全
- ✅ `docker image prune -a` 只删未被引用镜像，运行中的 frontend 不受影响
- ⚠️ 步骤6 备份清理只匹配 `data-*.tar.gz`，漏 campvideos-*（视频备份会无限累积）和旧 `studymate-*` 文件 → 建议扩展清理模式
- ⚠️ 旧 `studymate-data-2026-09-18_2125.tar.gz`(1.2G)+`studymate-campvideos-2026-09-18_2126.tar.gz`(157M) 是旧版脚本产物，与现内容重复，建议手动删腾 ~1.4G

**待用户验证**：① tar tzf data-20260918-224207.tar.gz | grep -E 'sqlite|camp-covers'（刚才命令被中断无输出；22:42 备份 728M 比 03:01 的 1.2G 小，需确认完整）② dbbd7f75 从权威卷补拷到 bind 目录的命令（上轮给，待执行）：DST=$(docker inspect -f '{{range .Mounts}}{{if eq .Destination "/app/data/camp-videos"}}{{.Source}}{{end}}{{end}}' studymate-frontend); docker run --rm -v studymate_studymate-frontend-data:/src:ro -v "$DST":/dst alpine sh -c 'cp -a /src/camp-videos/. /dst/'

## ✅ 全部闭环（2026-09-18 22:52）

用户跑完上面 4 步，输出确认：
- ① 22:42 数据备份完整：含 `studymate.sqlite`/`.sqlite-wal`/`.sqlite-shm` + `camp-covers/`（几十张封面）。**728M 担忧解除**——当时 tar 还在写，最终 `data-20260918-224207.tar.gz` = 1.2G 完整。
- ② 旧 `studymate-*` 重复备份已删，目录 11G→9.6G（腾 1.4G）。
- ③ bind 目录现 10 文件（9 mp4 + c2efbbc0.mov），含 `dbbd7f75-...mp4`(25M)，从权威卷补齐成功。
- ④ `dbbd7f75 -> 206`：**最后一个打不开的视频恢复**，8 个作品视频全部可播。

**数据保护总状态（全到位）**：DB+封面在命名卷→每日 data-*.tar.gz 轮转；视频在宿主机 bind→每日 campvideos-*.tar.gz 轮转；cron `0 3 * * *` 已生效；compose 红线禁 volume prune 已推生产；clean-disk.sh 无 volume prune 安全。后续可清裸卷 `studymate-frontend-data`（杂卷），勿动 `studymate_studymate-frontend-data`（现役数据卷）。
