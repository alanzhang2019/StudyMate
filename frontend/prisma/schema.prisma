// Minimal schema so `prisma generate` succeeds during Docker build.
// Add real models when wiring up persistence.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
