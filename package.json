{
  "name": "ai-math-mistake-machine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MVP backend scaffold for AI math mistake machine",
  "scripts": {
    "up": "npm install && npm run dev",
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
